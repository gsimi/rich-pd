// Number of global par in multi-peak fit
const static int n_glob_par = 3;

// Number of params per peak in multi-peak fit
const static int n_peak_par = 4;

Double_t pmtwavef(Double_t *x, Double_t *par);
